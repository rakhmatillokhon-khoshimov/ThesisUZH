# Thesis Draft

Build from this directory:

```bash
pdflatex main.tex
pdflatex main.tex
```

The exported thesis source expects the generated summary tables under
`../pilot_outputs/20260608/analysis_scaleup_cleaned/` and figures under
`../meeting_visuals/`.

The public export excludes raw private prompts, raw outputs, screenshots, and
second-coder packets. It is intended to compile the final thesis and expose the
summary-level analysis artifacts.
